export const metadata = {
  title: "Fila em Tempo Real",
  description: "Veja a fila em tempo real e entre para ser atendido rapidamente",
};

export default function Layout({ children }) {
  return children;
}
